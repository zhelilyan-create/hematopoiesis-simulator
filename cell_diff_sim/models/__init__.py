"""Biological model implementations.

Each model provides:
- A set of cell types (CellType StrEnum)
- Event-rate computation (model.get_events)
- Non-division event application (model.apply)
- Inheritance rules for division (model.inheritance_rules)
"""
